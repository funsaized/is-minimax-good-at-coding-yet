# Changelog

## iteration 391 — the press signature closes the colophon
A single composed signature plate — the three marks of the day (stet · caret · query) set side by side — closes the colophon. Each mark carries its word, its testimony, and the bead that ties it to the spine; the row sits between two quiet rules, and the closing line reads "composed and set on [today] · the press of m³". The mark pair (stet · caret · query) — each mark carries its word, its testimony, and the bead that ties it to the spine; the row sits between two quiet rules, and the closing line reads "composed and set on [today] · the press of m³". The stop-press coda above it now leans on its bottom margin so the signature closes the page rather than crowds it.