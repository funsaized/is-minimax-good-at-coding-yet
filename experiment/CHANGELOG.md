# Iteration 424

The compositor's lamp now hangs at the foot of the page. As the reader reaches the end of the broadside, a single warm bulb is lit over the closing panel — a hanging shade, a hand-drawn filament, a soft beam of light falling onto the page, a small constellation of stars scattered across the dark, and a compositor's note set on the lamp's table. The bulb glows in the active voice, the head rule and signature color with it, and the day that began at first light now closes after dark.

- **New closing panel** between the Reading Pouch and the site footer — a hanging composer's lamp, set in a quiet SVG composition with shade, bulb, filament, beam, constellation, and dust motes.
- **Intersection observer reveal** — the panel sits in soft opacity until the reader scrolls to it; the lamp then lights: cord draws down, shade settles, filament fades in, beam expands, stars twinkle, dust motes drift.
- **Voice-aware note** — a short compositor's line and a follow-up reading copy, each tuned to quiet / human / bold with matching typography (italic for quiet and human, roman for bold).
- **Self-contained SVG** — no remote fonts, no network assets; only local defs and gradients, and the existing voice-tone variable.

# Iteration history

- 410: an open composing breather now lives between every folio, holding the page as one continuous set-line.
- 411: the printer's atlas on the left margin now carries the day arc and the page-time.
- 412: the colophon folds into a single, signed line; the colophon seal reads in the voice.
- 413: the proof line is now set on a single cord; three voices read as three different stocks.
- 414: the dawn's halo now lifts off the page; the orb breathes across the top of the broadside.
- 415: the reader's pouch now holds three slips in three voices at the foot of the page.
- 416: a reader's pouch now lives at folio vi, between the colophon and the last light.
- 417: the redundant pre-hero scaffolding has been folded away.
- 418: a thin animated ink-line now lives between every folio.
- 419: the dawn arc that lived in the top-right now stretches across the whole top of the page.
- 420: added a printer's atlas on the left margin and tightened the colophon into a single, signed line.
- 421: the hero now sets the question alone on a single column.
- 422: the hero's centrepiece is now a real typesetter's specimen plate.
- 423: the hero closes as a single ledger line; the masthead names the day.
- 424: the compositor's lamp now hangs at the foot of the page, lit after dark.