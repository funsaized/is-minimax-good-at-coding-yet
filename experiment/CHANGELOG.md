The whole page, pulled — the press becomes the heart of the composition, and every folio registers the lever pull.

## Iteration 376

- Press state lifted to the page root: a global `pullSignal` drives a coordinated ripple through every folio.
- Topbar gains a small press proof slip — pulls counter, current voice letter, voice-tinted border — that briefly lights up on each pull.
- Global `app__ink-wash` and `app--pulling` class animate a soft ink swell across the page on each pull; the hero title briefly brightens with a voice-tinted glow.
- Marginalia slips now wobble on the cord (staggered by their pin position) when the lever is pulled; the active slip wobbles in place.
- The unbroken wax seal in the answer section taps and glows softly on each pull; the colophon seal does the same.
- Press impression's stamp now reads `impression № NNN · folio ii · at first light`, tying the day's pull count to the proof slip.
- The colophon signoff gains an impressions counter that tallies the day's pulls in the current voice tone — closing the journey with proof of work.
- Dawn orb and rays brighten briefly on each pull, so the first light reacts to the lever as well.
- All new animations respect `prefers-reduced-motion`.
