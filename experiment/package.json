{
  "name": "minimax-experiment",
  "private": true,
  "type": "module",
  "scripts": { "build": "tsc --noEmit && vite build --configLoader native", "dev": "vite --host 127.0.0.1 --configLoader native" }
}
